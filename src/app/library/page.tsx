import Image from 'next/image';
import Link from 'next/link';
import { ArrowUpRight, Play, Search } from 'lucide-react';
import { PageShell } from '@/components/page-shell';
import { Card, Pill } from '@/components/ui';
import { demoMeetings } from '@/lib/demo/data';

export default function LibraryPage(){
 const ended=demoMeetings.filter(m=>m.status==='ended');
 return <PageShell title="Biblioteca" kicker="Replay inteligente"><section className="h-full min-h-0 overflow-auto pb-8">
  <div className="apple-shell overflow-hidden rounded-[38px] p-6 md:rounded-[46px] md:p-10"><div className="flex flex-col gap-7 lg:flex-row lg:items-end lg:justify-between"><div><Pill>Replay inteligente</Pill><h1 className="mt-5 max-w-4xl text-5xl font-medium leading-[.92] tracking-[-.06em] md:text-7xl">Suas conversas,<br/><span className="text-black/25">organizadas para voltar.</span></h1><p className="mt-5 max-w-xl text-sm leading-6 text-black/45">Uma biblioteca limpa para rever reuniões, decisões e momentos importantes sem procurar em pastas.</p></div><div className="flex w-full max-w-sm items-center gap-2 rounded-full border border-black/10 bg-white px-4 py-3.5 shadow-sm"><Search size={16} className="text-black/30"/><input placeholder="Buscar reunião" className="w-full bg-transparent text-sm outline-none placeholder:text-black/30"/></div></div></div>
  <section className="mt-5 grid gap-5 lg:grid-cols-[1.2fr_.8fr]">{ended[0]&&<Link href={`/room/${ended[0].slug}`}><Card className="group overflow-hidden"><div className="relative min-h-[520px] overflow-hidden rounded-[32px] bg-black">{ended[0].thumbnailUrl&&<Image src={ended[0].thumbnailUrl} alt={ended[0].title} fill className="object-cover grayscale-[25%] opacity-75 transition duration-700 group-hover:scale-[1.025]"/>}<div className="absolute inset-0 bg-gradient-to-t from-black via-black/10 to-black/15"/><div className="absolute left-5 top-5 rounded-full border border-white/15 bg-black/25 px-3 py-2 text-[10px] uppercase tracking-[.16em] text-white/70 backdrop-blur-xl">Continuar assistindo</div><div className="absolute bottom-6 left-6 right-6 flex items-end justify-between gap-4"><div><h2 className="max-w-xl text-4xl font-medium tracking-[-.045em] text-white md:text-5xl">{ended[0].title}</h2><p className="mt-3 text-sm text-white/45">{ended[0].durationLabel} · replay</p></div><div className="grid size-14 place-items-center rounded-full bg-white text-black"><Play size={18} fill="currentColor"/></div></div></div></Card></Link>}<div className="grid gap-5">{ended.slice(1).map((m)=><Link href={`/room/${m.slug}`} key={m.id}><Card className="group overflow-hidden p-3"><div className="relative aspect-[16/10] overflow-hidden rounded-[24px] bg-black">{m.thumbnailUrl&&<Image src={m.thumbnailUrl} alt={m.title} fill className="object-cover grayscale-[30%] opacity-72 transition duration-500 group-hover:scale-[1.03]"/>}<div className="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent"/></div><div className="flex items-start justify-between gap-4 p-3 pt-5"><div><h3 className="text-xl font-semibold tracking-[-.03em]">{m.title}</h3><p className="mt-2 text-xs text-black/36">{m.durationLabel} · {new Intl.DateTimeFormat('pt-BR',{day:'2-digit',month:'short'}).format(new Date(m.scheduledAt))}</p></div><div className="grid size-10 place-items-center rounded-full border border-black/10 bg-white"><ArrowUpRight size={16}/></div></div></Card></Link>)}</div></section>
 </section></PageShell>
}
