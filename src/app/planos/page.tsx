import { Check, Crown, Sparkles, UsersRound } from 'lucide-react';
import { PageShell } from '@/components/page-shell';
import { octaPlans } from '@/lib/plans';

const icons={free:Sparkles,pro:Crown,premium:UsersRound};
export default function PlanosPage(){return <PageShell title="Planos e preços" kicker="Escolha o nível ideal para você"><div className="grid h-full grid-cols-1 gap-4 md:grid-cols-3">{octaPlans.map(plan=>{const Icon=icons[plan.id];return <article key={plan.id} className={`octa-plan-card min-h-[590px] ${plan.featured?'is-featured':''}`}><div className="flex items-start justify-between"><span className="grid size-11 place-items-center rounded-2xl bg-[#eaf3f5] text-[#0b7285]"><Icon size={20}/></span>{plan.featured&&<span className="rounded-full bg-[#0b7285] px-3 py-1 text-[10px] font-semibold uppercase tracking-[.12em] text-white">Mais escolhido</span>}</div><h2 className="mt-5 text-2xl font-semibold tracking-[-.04em] text-[#0a2238]">{plan.name}</h2><div className="mt-3"><strong className="block text-[24px] font-semibold leading-tight tracking-[-.04em] text-[#0a2238]">{plan.priceLabel}</strong>{plan.annualLabel&&<span className="mt-1 block text-[11px] leading-4 text-[#667b8e]">{plan.annualLabel}</span>}</div><p className="mt-3 min-h-14 text-sm leading-5 text-[#667b8e]">{plan.description}</p><div className="mt-5 space-y-2.5">{plan.features.map(f=><div key={f} className="flex items-start gap-2 text-[12px] leading-4 text-[#29465d]"><span className="mt-0.5 grid size-5 shrink-0 place-items-center rounded-full bg-[#e7f3f0] text-[#14755f]"><Check size={12}/></span><span>{f}</span></div>)}</div><button className={plan.featured?'octa-primary-button mt-auto w-full':'octa-secondary-button mt-auto w-full'}>{plan.id==='free'?'Começar grátis':'Escolher '+plan.name}</button><p className="mt-2 text-center text-[10px] text-[#8495a3]">Configuração comercial provisória.</p></article>})}</div></PageShell>}
